(function () {
    "use strict";

    /**
     * @ngdoc function
     * @name sy.UserJoin.service : sy.UserJoinSvc
     * 비밀번호 재설정 등록
     */
    angular.module("sy.UserJoin.service")
        .factory("sy.UserJoinSvc", ["APP_CONFIG", "$http", function (APP_CONFIG, $http) {
            return {
            };
        }]);
}());