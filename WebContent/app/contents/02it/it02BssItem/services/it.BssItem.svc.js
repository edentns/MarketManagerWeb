(function () {
    "use strict";

    /**
     * @ngdoc function
     * @name it.BssItem.service : it.BssItemSvc
     * 기본상품관리
     */
    angular.module("it.BssItem.service")
        .factory("it.BssItemSvc", ["APP_CONFIG", "$http", function (APP_CONFIG, $http) {
            return {
            };
        }]);
}());