(function () {
    "use strict";

    /**
     * @ngdoc function
     * @name it.ItemCfct.service : it.ItemCfctSvc
     * 상품분류관리
     */
    angular.module("it.ItemCfct.service")
        .factory("it.ItemCfctSvc", ["APP_CONFIG", "$http", function (APP_CONFIG, $http) {
            return {
            };
        }]);
}());